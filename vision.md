# Markdown编辑器项目

编辑区功能:

1. 使用Monaco Editor作为编辑器组件,放在页面左侧。
2. 支持Markdown语法高亮、自动补全、代码折叠等功能。
3. 编辑时实时将内容同步到一个数据对象中,供渲染区使用。
4. 支持常用的编辑器快捷键,如tab缩进、ctrl+z撤销等。
5. 支持设置字体大小、行号、主题等编辑器选项。
6. 支持搜索和替换功能。
7. 支持快速插入表格、链接、图片等常用Markdown元素。

渲染区功能:

1. 使用markdown-it库将编辑区的Markdown内容转换为HTML。
2. 支持github flavored markdown语法。
3. 支持各种扩展语法:
   - 自定义容器: ::: 语法
   - Emoji: :emoji: 语法
   - 表格: table,tr,th,td语法  
   - 任务列表: - [ ] 语法
   - 脚注: [^footnote] 语法
   - 定义列表: term
   - 删除线: ~~delete~~ 语法
   - 下划线: _underline_ 语法  
   - 数学公式: $equation$ 语法
   - 自动目录语法：@[TOC](这里写目录标题)
   - 支持多层级代码块，比如普通代码块为```，但是可以通过增加`的个数来嵌套层级
4. 代码块语法高亮,支持多种常见语言如js/ts/python/java/c等。
5. 支持导入和渲染Echarts图表配置。
6. 将渲染后的HTML内容显示在页面右侧。
7. 支持渲染LaTex数学公式,使用KaTeX库。  
8. 支持渲染流程图/时序图/甘特图等,使用最新Mermaid库（```mermaid、````mermaid、...，不论多少层级，即代码块语言为mermaid）。
9. 支持导出为PDF、PNG、JPG等格式。
10. 支持渲染echarts的option,使用最新echarts库（```echarts、````echarts、、...，不论多少层级，即代码块语言为echarts）

同步滚动功能:

1. 编辑区和渲染区默认同步滚动,保持一致的阅读位置。
2. 同步滚动的实现是计算两侧内容的滚动比例,然后相互设置scrollTop。
3. 提供选项允许关闭同步滚动。
4. 如果关闭同步,则两侧独立滚动,互不影响。
5. 支持为特定元素如标题、图片等添加锚点,点击可快速定位。

界面布局:

1. 整体采用左右分栏布局,左侧编辑区,右侧渲染区。
2. 栏宽可以拖动调整,需要实现一个分隔条组件。
3. 顶部可以放一些全局的工具栏按钮,如保存、主题切换、预览等。
4. 底部可以放一些状态栏信息,如字数、行号、缩放比例等。
5. 支持切换明亮和暗黑两种主题模式。

文件处理:

1. 支持读取本地的Markdown文件到编辑器中。
2. 支持将编辑器内容保存为本地Markdown文件。
3. 支持在线拖拽上传图片并插入到编辑器光标位置。
4. 支持从URL或剪贴板粘贴图片。

编辑历史管理:

1. 自动记录每次编辑操作,可以撤销(undo)和重做(redo)。
2. 支持自定义快捷键触发撤销重做。
3. 对于撤销和重做,应该是基于Markdown语法级别的,而不是字符级别。

大纲导航:

1. 自动提取Markdown文档中的标题(h1~h6)生成大纲。
2. 大纲在编辑区和渲染区都要显示,并且可以点击快速定位到对应内容。
3. 大纲应该随着文档内容的变化实时更新。
可以基于对象对象采用各种设计模式来使得代码逻辑更加清晰，代码
代码设计可以巧妙运用各种设计模式，使得不同扩展语法可以轻松灵活地插拔式使用。通过编辑器的.use(plugin)方法，可以安装编辑器中间件来实现某些类别的扩展功能的处理逻辑，自带扩展语法就是默认的中间件。
借鉴java项目、flutter项目等，是怎么规划的。最重要的是结合本工程的要求和面向对象与设计模式最佳实践来规划。后续更多得逻辑会遵循面向对象方法，如果你借鉴flutter项目规划就容易理解我的想法了，这个意义上我比较倾向于使用tsx语法。
因此先给出此vue项目完整的目录和文件规划，如下：
